<snippet>
	<content><![CDATA[
CONSTRAINT ${1:name} FOREIGN KEY (${2:column}) REFERENCES ${3:table}(${4:primaryKeyColumn})
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>CONSTRAINT-FOREIGN-KEY</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.sql</scope>
	<description>CONSTRAINT name PRIMARY FOREIGN KEY (column) ...</description>
</snippet>